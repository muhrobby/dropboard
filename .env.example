# Database Configuration - External PostgreSQL
DATABASE_URL=postgresql://postress:aX0K6Dz4pHEP5Jj71dy5@43.133.147.104:5433/Postgress

# Application Configuration
APP_PORT=3004
NEXT_PUBLIC_APP_URL=http://localhost:3004

# Better Auth Configuration
# Generate a random secret with: openssl rand -base64 32
BETTER_AUTH_SECRET=your_better_auth_secret_here_generate_with_openssl
BETTER_AUTH_URL=http://localhost:3004

# Signed URL Configuration
# Generate a random secret with: openssl rand -base64 32
# Used for secure file download URLs
SIGNED_URL_SECRET=your_signed_url_secret_here_generate_with_openssl

# CORS Configuration (comma-separated list of allowed origins)
NEXT_PUBLIC_ALLOWED_ORIGINS=http://localhost:3004,https://yourdomain.com

# Node Environment
NODE_ENV=production
